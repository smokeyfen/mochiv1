# Batch 06 — R0-B apps/web Project Input Surface

Date: 2026-09-06

## Checkpoint

- Branch: `codex/feasibility-lock-candidate`
- Starting checkpoint: `1e1252bd35acbb8a045b065d24e8fa4f41964918`
- R0-B implementation commit: `5e807ec160e7e05d8d74d2e9e0e9c65d961222ac`
- Status: PASS

## Files changed

- `apps/web/package.json`
- `apps/web/src/App.tsx`
- `apps/web/src/App.test.tsx`
- `apps/web/src/styles.css`
- `package-lock.json`
- `README.md`
- `STATUS.md`
- `docs/CODEX_NEXT_MILESTONES.md`
- `docs/progress/BATCH_06_R0_B_INPUT_SURFACE.md`

No Contract, Core, Provider, harness, backend, or production-runtime source changed.

## Behavior delivered

- The single user-facing surface is now a minimal responsive form that builds `MochiProjectInput` with the existing `@mochi/contracts` types and `validateMochiProjectInput`.
- `projectId` and `productId` are stable logical IDs generated by the application; they are not user inputs.
- Product images can be selected in multiple, shown as thumbnails, assigned a product reference role, and removed.
- Browser `File` values are stored only in a runtime `assetId → File` map. Object URLs are preview-only and are revoked when removed or on component disposal.
- The canonical preview appears only after validation as `READY_FOR_ANALYSIS`; it contains neither Files, Blob/object URLs, local paths, credentials, provider IDs, nor provider settings.

## Commands executed

- `npm install -w @mochi/web`
- `npm run typecheck`
- `npm test`
- `npm run benchmark:dry`
- `npm run build -w @mochi/web`

## Verification

- Typecheck: PASS across all five workspaces.
- Tests: PASS, 34 tests total. Web has 7 tests covering form fields, fail-closed submission, canonical placement of audience, voice values, logical image assets, role changes/removal, sanitized preview, and absent provider actions.
- Dry benchmark: PASS with expected fail-closed result. `PICK_UP` remains `UNTESTED`; feasibility remains `passed: false` with `action_untested:PICK_UP`.
- Web build: PASS.
- Gemini calls: 0.
- Flow calls: 0.
- Real video generation count: 0.
- Main: unchanged at `75fd0e5d80531de9d1bb8cbec9899c32d269fab4`.

## Architecture deviations

None. R0-A contracts are unchanged. The browser does not call a provider, create Product Evidence, reason, generate video, synthesize voice, plan scenes, run QC, or create a backend/persistence layer.

## Next action

STOP. R1 Product Evidence is NOT STARTED. Google Flow remains the only planned video renderer using Flow credits, and integration remains deferred until the reasoning pipeline is stable.
