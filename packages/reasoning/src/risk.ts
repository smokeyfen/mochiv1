import { type StateResolved4ScenePlan } from '@mochi/contracts'; import { type ActionCapabilityMap } from '@mochi/core'; import { ACTION_SEMANTICS } from './state.ts';
export type SceneRiskStatus='READY'|'CONDITIONAL'|'BLOCKED'; export interface SceneRiskAssessment{sceneId:string;index:number;actionId:string;actionCapability:string;actionComplexity:number;referenceQuality:'READY'|'LIMITED';status:SceneRiskStatus;reasons:string[];warnings:string[];replanRecommended:boolean;} export interface GlobalSceneRiskAssessment{scenes:SceneRiskAssessment[];}
export function evaluateSceneRisk(plan:StateResolved4ScenePlan,map:ActionCapabilityMap):GlobalSceneRiskAssessment{const limited=plan.referenceReadiness==='LIMITED';return{scenes:plan.scenes.map(s=>{const cap=map[s.primaryAction],complexity=ACTION_SEMANTICS[s.primaryAction].complexity;let status:SceneRiskStatus='READY',reasons:string[]=[],warnings:string[]=[];if(cap==='AVOID'){status='BLOCKED';reasons.push('action_avoid');}else if(cap==='UNTESTED'||cap==='RISKY'){status='CONDITIONAL';reasons.push(`action_${cap.toLowerCase()}`);}else if(complexity===3){status='CONDITIONAL';reasons.push('complexity_3');}if(limited){warnings.push(...plan.referenceLimitations);if(status!=='BLOCKED'&&complexity>1)status='CONDITIONAL';}return{sceneId:s.sceneId,index:s.index,actionId:s.primaryAction,actionCapability:cap,actionComplexity:complexity,referenceQuality:limited?'LIMITED':'READY',status,reasons,warnings,replanRecommended:status!=='READY'};})};}
