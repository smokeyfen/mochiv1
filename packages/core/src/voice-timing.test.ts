import assert from 'node:assert/strict';import test from 'node:test';import type { VoiceTimingCalibrationKey,VoiceTimingCalibrationPolicy,VoiceTimingObservation } from '@mochi/contracts';import { assertEmpiricalVoiceTimingProfile,buildVoiceTimingProfile,countVietnameseSpokenUnits,estimateDialogueDuration,normalizeVietnameseSpokenText,VoiceTimingError } from './index.ts';
const key:VoiceTimingCalibrationKey={language:'vi-VN',voiceGender:'FEMALE',voiceRegion:'SOUTH',voiceStyle:'warm'};const policy:VoiceTimingCalibrationPolicy={targetSceneDurationMs:8000,safetyMarginMs:1000,minimumObservations:4};const obs=(id:string,text:string,ms:number,k=key,provenance:'SYNTHETIC'|'EMPIRICAL'='SYNTHETIC'):VoiceTimingObservation=>({observationId:id,calibrationKey:k,normalizedText:normalizeVietnameseSpokenText(text),spokenUnitCount:countVietnameseSpokenUnits(text),measuredDurationMs:ms,provenance});const samples=()=>[obs('a','xin chào bạn',1000),obs('b','xin chào bạn',1500),obs('c','xin chào bạn',2000),obs('d','xin chào bạn',3000)];
test('Vietnamese spoken-unit normalization is deterministic and punctuation cannot create units',()=>{const input='  Xin,   chào!!!  bạn... ';assert.equal(normalizeVietnameseSpokenText(input),'Xin chào bạn');assert.equal(normalizeVietnameseSpokenText(input),normalizeVietnameseSpokenText(input));assert.equal(countVietnameseSpokenUnits(input),3);assert.equal(countVietnameseSpokenUnits('  ... !!!  '),0);});
test('T0 observation validation fails closed for invalid fields and duplicate IDs',()=>{const invalid={...obs('','xin chào',1000),spokenUnitCount:99};assert.throws(()=>buildVoiceTimingProfile([invalid,obs('b','xin chào',1000),obs('c','xin chào',1000),obs('d','xin chào',1000)],policy),(e:unknown)=>e instanceof VoiceTimingError&&e.code==='INVALID_OBSERVATION');const duplicate=[obs('a','xin chào',1000),obs('a','xin chào',1100),obs('c','xin chào',1200),obs('d','xin chào',1300)];assert.throws(()=>buildVoiceTimingProfile(duplicate,policy));});
test('T0 rejects mixed exact voice identity, language, insufficient samples, and invalid margins',()=>{for(const changed of [{...key,voiceGender:'MALE' as const},{...key,voiceRegion:'NORTH' as const},{...key,voiceStyle:'formal'},{...key,language:'en-US' as any}]){const data=samples();data[3]={...data[3]!,calibrationKey:changed};assert.throws(()=>buildVoiceTimingProfile(data,policy),(e:unknown)=>e instanceof VoiceTimingError);}assert.throws(()=>buildVoiceTimingProfile(samples().slice(0,3),policy));assert.throws(()=>buildVoiceTimingProfile(samples(),{...policy,safetyMarginMs:8000}));});
test('T0 profile uses deterministic median and lower-quartile conservative rate',()=>{const profile=buildVoiceTimingProfile(samples(),policy);assert.equal(profile.targetSceneDurationMs,8000);assert.equal(profile.usableSpeechDurationMs,7000);assert.equal(profile.medianUnitsPerSecond,1.5);assert.equal(profile.conservativeUnitsPerSecond,1);assert.equal(profile.recommendedMaxSpokenUnits,7);assert.deepEqual(buildVoiceTimingProfile(samples(),policy),profile);assert.equal(profile.calibrationKey.voiceGender,'FEMALE');assert.equal(profile.calibrationKey.voiceRegion,'SOUTH');assert.equal(['providerId','media','dataBase64','credential','executablePath'].some(key=>key in profile),false);});
test('T0 duration estimator preserves text and has deterministic FITS/TOO_LONG boundary',()=>{const profile=buildVoiceTimingProfile(samples(),policy),fits=estimateDialogueDuration('xin chào bạn bạn bạn bạn bạn',profile),long=estimateDialogueDuration('xin chào bạn bạn bạn bạn bạn bạn',profile);assert.equal(fits.status,'FITS');assert.equal(long.status,'TOO_LONG');assert.equal(fits.normalizedText,'xin chào bạn bạn bạn bạn bạn');});
test('all V1 gender and region combinations are representable and synthetic profiles are not empirical',()=>{for(const gender of ['MALE','FEMALE'] as const)for(const region of ['SOUTH','NORTH'] as const){const p=buildVoiceTimingProfile(samples().map(o=>({...o,calibrationKey:{...key,voiceGender:gender,voiceRegion:region}})),policy);assert.equal(p.calibrationKey.voiceGender,gender);assert.equal(p.calibrationKey.voiceRegion,region);assert.throws(()=>assertEmpiricalVoiceTimingProfile(p),(e:unknown)=>e instanceof VoiceTimingError&&e.code==='NOT_EMPIRICAL');}const empirical=buildVoiceTimingProfile(samples().map(o=>({...o,provenance:'EMPIRICAL' as const})),policy);assert.doesNotThrow(()=>assertEmpiricalVoiceTimingProfile(empirical));});
